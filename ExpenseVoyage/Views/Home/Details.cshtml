@model ExpenseVoyage.Viewmodels.HomeViewModel

@* <div class="container">
    <h1>@Model.Destinations?.Name</h1>
    <p>: @Model.Destinations?.Description</p>
    <img src="@Model.ImageUrl" alt="@Model.Destinations?.Name" class="img-fluid" />
    <p>Description: @Model.Destinations?.Description</p>
</div> *@
<div class="untree_co-section">
    <div class="container">
        <div class="row justify-content-between align-items-center">
            <div class="col-lg-6">
                <figure class="img-play-video">
                    <a id="play-video" class="video-play-button" href="https://www.youtube.com/watch?v=mwtbEGNABWU" data-fancybox>
                        <span></span>
                    </a>
                    @foreach (var item in Model.DestinationImages)
                    {

                            <img src="~/Uploads/@item.ImageUrl" alt="Image" class="img-fluid rounded-20" />

                    }
                </figure>
            </div>

            <div class="col-lg-5">
                <h2 class="section-title text-left mb-4">Take a look at @Model.Destinations.Name Video</h2>
                <p>@Model.Destinations?.Description</p>
                <p><a href="#" class="btn btn-primary">Get Started</a></p>
            </div>
        </div>
    </div>
</div>